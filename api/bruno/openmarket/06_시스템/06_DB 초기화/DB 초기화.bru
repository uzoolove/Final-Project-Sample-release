meta {
  name: DB 초기화
  type: http
  seq: 1
}

post {
  url: {{url}}/db/init
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  initData: @file()
  attach: @file()
}

settings {
  encodeUrl: true
}
